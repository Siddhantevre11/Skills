---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
disable-model-invocation: true
---

Run a `/grilling` session, using the `/domain-modeling` skill.

## How I use this

My entry point for every new project instead of plan mode -- plan mode plans without pressure-testing, this actually interrogates the "why" behind each decision until the system design is settled, before any code gets written.